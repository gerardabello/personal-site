{
  "name": "gerardabello",
  "description": "",
  "devDependencies": {
    "babel-core": "^6.18.2",
    "babel-loader": "^6.2.8",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.16.0",
    "standard": "^8.6.0",
    "webpack": "^3.2.0",
    "webpack-dev-server": "^2.5.1"
  },
  "dependencies": {
    "react": "^16.0.0-alpha.13",
    "react-dom": "^16.0.0-alpha.13",
    "styled-components": "^2.1.1"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "start": "webpack-dev-server",
    "build": "npm run clean && NODE_ENV=production webpack && cp ./index.html ./dist && cp -r ./assets ./dist",
    "lint": "standard --verbose",
    "deploy": "npm run build && surge -p dist -d gerardabello.surge.sh"
  },
  "license": "UNLICENSED",
  "standard": {
    "global": [
      "it",
      "xit",
      "describe",
      "xdescribe",
      "beforeEach",
      "afterEach"
    ],
    "ignore": [
      "/node_modules/"
    ]
  },
  "babel": {
    "presets": [
      "es2015",
      "react"
    ]
  }
}
